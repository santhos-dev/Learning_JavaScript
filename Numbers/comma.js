"use strict";

console.log("Comma");
// ------------------------------------------------------------

// Most rarest and unusual operator

// Each of them is evaluated but only the result of last one is returned

let a = (1 + 2, 3 + 5);
console.log(a); //  gives last evaluated vale 3 + 5 = 8

// It has very lower precendence than = assigmment operator so we use parentheses to evaluate with comma operator

// For tricky expression

// for (/*a = 1, b = 2, c = a * b;*/ a < 10; a++){

// 
let s = 0;
console.log(s);

